<%= form_for(@thing_with_error) do |f| %>
  <% if @thing_with_error.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thing_with_error.errors.count, "error") %> prohibited this thing_with_error from being saved:</h2>

      <ul>
      <% @thing_with_error.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
